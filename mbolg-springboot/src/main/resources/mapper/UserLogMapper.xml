<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="work.moonzs.mapper.UserLogMapper">
    <select id="listWeeklyVisits" resultType="java.util.Map">
        SELECT COUNT(DISTINCT ip) AS ip, COUNT(ip) AS access, DATE_FORMAT(create_time, '%Y-%m-%d') createTime
        FROM t_user_log
        WHERE DATE_FORMAT(create_time, '%Y-%m-%d') > #{date}
        GROUP BY DATE_FORMAT(create_time, '%Y-%m-%d')
    </select>
</mapper>